import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'kz.nuxt3capacitor',
  appName: 'nuxt3capacitor',
  webDir: '.output/public',
  bundledWebRuntime: false,
}

export default config
